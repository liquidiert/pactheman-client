[opcode(0x5)]
struct PlayerState {
    SessionMsg session;
    bool ready;
    int64 reconciliationId;
    map[guid, int64] score;
    map[guid, int64] lives;
    map[string, Position] ghostPositions;
    map[guid, Position] playerPositions;
}