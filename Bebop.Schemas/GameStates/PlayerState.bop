[opcode(0x5)]
struct PlayerState {
    SessionMsg session;
    MovingStates direction;
    map[guid, int64] scores;
    map[guid, int64] lives;
    map[guid, Position] playerPositions;
    array[Position] scorePositions;
}